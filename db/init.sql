-- Extra indexes for multi-tenant query patterns (created after tables by SQLAlchemy,
-- but we also declare them here for clarity and early creation on fresh volumes)

-- These will be idempotent / ignored if already present via SQLAlchemy models
